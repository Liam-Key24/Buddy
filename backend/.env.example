MODELS_DIR=../brain/models
# Preferred replacement once acceptance-tested (do not delete Qwen3-14B until then):
# mlx-community/Qwen3.5-4B-4bit (~3.0 GB) or an mlx-lm text conversion such as
# Siarhei/Qwen3.5-4B-4bit (~2.4 GB). Store under brain/models/ with HF_HUB_CACHE
# pointed at that directory to avoid a second Hugging Face copy.
BUDDY_MODEL_PATH=../brain/models/Qwen3-14B-4bit
