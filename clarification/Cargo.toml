[package]
name = "buddy-clarification"
version.workspace = true
edition.workspace = true

[dependencies]
buddy-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
buddy-core = { path = "../core" }
